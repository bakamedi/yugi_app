enum HttpResult {
  ok,
  accessDenied,
  networkError,
  socketException,
  socketTimeout,
  notFound,
  unknownError,
  invalidGrant,
  proxyError,
  noInternetConnection,
  serviceUnavailable,
  partialContent,
}
